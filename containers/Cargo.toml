[package]
name = "containers"
version = "0.1.0"
edition = "2021"

[lib]
name = "containers"
path = "src/lib.rs"

[dependencies]
qvek = { version = "^0.1.0" }
# how tf there is no flag to ignore this?
# qvek = { path = "../../qvek/qvek" }
