cargo-features = ["edition2024"]

[package]
name = "diffuse"
version = "0.1.0"
edition = "2024"

[lib]
path = "diffuse.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
