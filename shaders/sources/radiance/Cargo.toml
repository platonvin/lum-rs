cargo-features = ["edition2024"]

[package]
name = "radiance"
version = "0.1.0"
edition = "2024"

[lib]
path = "radiance.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
