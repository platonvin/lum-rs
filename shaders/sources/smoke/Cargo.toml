cargo-features = ["edition2024"]

[package]
name = "smoke"
version = "0.1.0"
edition = "2024"

[lib]
path = "smoke.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
