cargo-features = ["edition2024"]

[package]
name = "water_vert"
version = "0.1.0"
edition = "2024"

[lib]
path = "water_vert.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
