cargo-features = ["edition2024"]

[package]
name = "lightmap_models"
version = "0.1.0"
edition = "2024"

[lib]
path = "lightmap_models.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
