cargo-features = ["edition2024"]

[package]
name = "lightmap_blocks"
version = "0.1.0"
edition = "2024"

[lib]
path = "lightmap_blocks.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
