cargo-features = ["edition2024"]

[package]
name = "perlin"
version = "0.1.0"
edition = "2024"

[lib]
path = "perlin.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
