cargo-features = ["edition2024"]

[package]
name = "update_water"
version = "0.1.0"
edition = "2024"

[lib]
path = "update_water.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
common = { path = "../../common" }
